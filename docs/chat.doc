Chat
============
+Row[] rows
	A list of all the currently active rows of comments
	
+run()
	Scrolls all rows
+addComment(String message)
	Picks a row (Randomly, sequential order, whichever row is backlogged the least)
	Adds a comment to the list of comments to that row